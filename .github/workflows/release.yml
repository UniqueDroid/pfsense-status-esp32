name: Build and attach firmware release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install PlatformIO
        run: python -m pip install --upgrade pip platformio

      - name: Preinstall project dependencies
        run: pio pkg install -e lilygo-t-display-s3

      - name: Apply patched WiFiManager files
        run: |
          cp third_party/wifimanager-patched/WiFiManager.h .pio/libdeps/lilygo-t-display-s3/WiFiManager/WiFiManager.h
          cp third_party/wifimanager-patched/WiFiManager.cpp .pio/libdeps/lilygo-t-display-s3/WiFiManager/WiFiManager.cpp
          cp third_party/wifimanager-patched/wm_strings_en.h .pio/libdeps/lilygo-t-display-s3/WiFiManager/wm_strings_en.h

      - name: Generate build version header
        shell: bash
        run: |
          version="${GITHUB_REF_NAME}"
          if [ -z "$version" ]; then
            version="dev"
          fi
          mkdir -p include
          cat > include/build_version.h <<EOF
          #pragma once
          #define BUILD_VERSION "$version"
          EOF

      - name: Build firmware
        run: pio run -e lilygo-t-display-s3

      - name: Rename firmware artifact
        shell: bash
        run: |
          cp .pio/build/lilygo-t-display-s3/firmware.bin pfsense-status-esp32-lilygo-t-display-s3-${GITHUB_REF_NAME}.bin

      - name: Create or update release
        uses: softprops/action-gh-release@v2
        with:
          files: pfsense-status-esp32-lilygo-t-display-s3-${{ github.ref_name }}.bin
          generate_release_notes: true
          fail_on_unmatched_files: true